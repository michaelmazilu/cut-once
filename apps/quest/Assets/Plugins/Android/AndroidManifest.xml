<?xml version="1.0" encoding="utf-8" standalone="no"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools" android:installLocation="auto" xmlns:horizonos="http://schemas.horizonos/sdk">
  <!-- The server is Michael's laptop on the room's Wi-Fi (http://<ip>:8080) or a quick tunnel. Android 9+ blocks
       cleartext HTTP by default, which would make every request fail on the headset while working in the Editor. -->
  <application android:label="@string/app_name" android:icon="@mipmap/app_icon" android:allowBackup="false" android:usesCleartextTraffic="true">
    <activity android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" android:configChanges="locale|fontScale|keyboard|keyboardHidden|mcc|mnc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode" android:launchMode="singleTask" android:name="com.unity3d.player.UnityPlayerGameActivity" android:excludeFromRecents="true" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
        <category android:name="com.oculus.intent.category.VR" />
      </intent-filter>
      <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />
    </activity>
    <meta-data android:name="unityplayer.SkipPermissionsDialog" android:value="false" />
    <meta-data android:name="com.oculus.handtracking.frequency" android:value="LOW" />
    <meta-data android:name="com.oculus.ossplash.background" android:value="black" />
    <meta-data android:name="com.oculus.telemetry.project_guid" android:value="6fd2171b-b79e-4703-a480-052c683e4e28" />
    <meta-data android:name="com.oculus.supportedDevices" android:value="quest3|quest3s" tools:replace="android:value" />
  </application>
  <uses-feature android:name="android.hardware.vr.headtracking" android:version="1" android:required="true" />
  <uses-feature android:name="oculus.software.handtracking" android:required="false" />
  <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />
  <uses-permission android:name="com.oculus.permission.USE_ANCHOR_API" />
  <uses-permission android:name="com.oculus.permission.USE_SCENE" />
  <uses-feature android:name="com.oculus.feature.PASSTHROUGH" android:required="true" />
  <horizonos:uses-horizonos-sdk horizonos:minSdkVersion="60" horizonos:targetSdkVersion="205" />
  <uses-permission android:name="horizonos.permission.HEADSET_CAMERA" />
  <!-- The copilot's push-to-talk. Declared explicitly: a runtime request for an undeclared permission is refused without a dialog. -->
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <!-- Spatial data, for depth rays (build mode's scan, placing on a real table). Meta's build step adds it too while scene support is on
       (OculusProjectConfig); declared here as well so every permission the app asks for at runtime is in plain sight. -->
  <uses-permission android:name="com.oculus.permission.USE_SCENE" />
</manifest>
